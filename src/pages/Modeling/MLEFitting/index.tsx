import React from 'react';
import PageTemplate from '@/components/common/PageTemplate';

const MLEFittingPage: React.FC = () => {
  return (
    <PageTemplate
      title="Maximum Likelihood Fitting"
      description="Fit models to data using maximum likelihood estimation"
      category="Modeling"
      status="coming-soon"
    />
  );
};

export default MLEFittingPage;
